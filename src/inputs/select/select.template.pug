mat-form-field.rui-input-container
	mat-select([placeholder]='fieldData.placeholder', [formControl]='fieldData.inputFormControl') 
		mat-option(*ngFor='let option of fieldData.selectList', [value]="option.value") {{option.text}}
	include ../base/baseInput.commonElementsTemplate.pug